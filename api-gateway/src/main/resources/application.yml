server:
  port: 4004

spring:
  main:
    web-application-type: reactive

  cloud:
    gateway:
      routes:
        - id: product-service-route
          uri: http://product-service:8080
          predicates:
            - Path=/api/products/**
          filters:
            - StripPrefix=1